[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  # Redirects frontend api calls to Netlify functions
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
